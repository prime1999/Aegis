We will use the gemini AI to deeply work on the result of the scan to get the protocols involed and there names

## Set up AI functionality

Create a new api route

- In the api folder create `/api/aiWalletAnalyzer/route` and start working on the logic
- The AI using gemini AI sould thing like expert blockchain forensics and intelligence agent
- It analyze the result gotten from the wallet scan and then underlysing protocol and ecosystem, the API key is in the .env file and @google/genai is installed
- log the result to the console

Do not attach any functionalities after the Scanning

### Check when done

- new components compile without TypeScript errors
- no lint errors
- Each result is checked and the AI analyzes the all result in the array
