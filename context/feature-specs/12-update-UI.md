We create a UI to hold the analyzer update result

## Create another sidebar

Create a sidebar to hold the UI

- The sidebar `/components/updatesSidebar` will stay at the right of the body and will be design just like the current sidebar.
- The result of the updates will be used, but the body will just show some part of it since we will have a button that will say view update
- the view update will open a shadcn dialog which will show the full details of tat specific update.

Do not attach any other functionalities or UI

### Check when done

- new components compile without TypeScript errors
- no lint errors
