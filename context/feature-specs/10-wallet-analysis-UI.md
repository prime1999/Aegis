We will create the UI to display the AI-Analysis to the user

## Create Sidebar component

We will create a container at the left of the screen beside the AI image

- This container will old the analzed result from te AI
- Make the container has a glassmorphism
- add animation using gsap to animate the container in
- Also add animation to animate the results in after the container is in.
- add an icon based on if the transaction category is external, erc20, erc721

Do not attach any other functionalities or UI

### Check when done

- new components compile without TypeScript errors
- no lint errors
- Also the layout is responsive on all screen sizes
